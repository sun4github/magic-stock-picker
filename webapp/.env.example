# Copy to .env and set your PostgreSQL connection (same DB the agent writes to).
DATABASE_URL=postgresql://user_ai:YOUR_PASSWORD_HERE@192.168.1.190:5432/stock_agent_db

# Port the web app listens on (default 8000).
PORT=8000
